//5. Write a program to find the ASCII value of a character.

public class ASCII {
    public static void main(String[] args) {
        char ch = 'A';
        int ascii = ch; // or (int) ch
        System.out.println("ASCII value of " + ch + " is " + ascii);
    }
}
